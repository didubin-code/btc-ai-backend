{
  "name": "btc-ai-backend-v73-3-streamguard",
  "version": "73.3.0",
  "private": true,
  "description": "BTC Signal AI copilot backend with stale stream guard and local fallback.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node --test ../tests/*.test.js",
    "check": "node --check server.js && node --check logic.js"
  },
  "engines": {
    "node": ">=18"
  }
}
